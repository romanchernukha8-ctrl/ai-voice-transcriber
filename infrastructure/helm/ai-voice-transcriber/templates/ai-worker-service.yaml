apiVersion: v1
kind: Service
metadata:
  name: ai-worker
  labels:
    app.kubernetes.io/name: ai-worker
spec:
  selector:
    app.kubernetes.io/name: ai-worker
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
