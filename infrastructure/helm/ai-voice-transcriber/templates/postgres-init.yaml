apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ai-voice-transcriber.fullname" . }}-postgres-init
  labels:
    {{- include "ai-voice-transcriber.labels" . | nindent 4 }}
data:
  init.sql: |
    CREATE DATABASE file_db;
