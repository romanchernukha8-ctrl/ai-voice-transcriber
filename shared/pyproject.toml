[project]
name = "shared"
version = "0.1.0"
description = "Shared library for AI Voice Transcriber microservices"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
